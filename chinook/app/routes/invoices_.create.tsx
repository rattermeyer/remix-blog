import { Form } from "@remix-run/react";

export default function Invoices_Create() {
	return (
		<>
			<h1 id="create">Create Invoice</h1>
			<p>This is the page where you can create a new invoice.</p>
		</>
	);
}
